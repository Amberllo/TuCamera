<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <ImageView
        android:id="@+id/welcome_img"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"/>

    <Button
        android:id="@+id/welcome_comein_btn"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="40dp"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:scaleType="fitCenter"
        android:text="@string/welcome_quickstart"
        android:background="@drawable/selector_btn_rectangle"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="@color/green_common"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true">
    </Button>

    <TextView
        android:textStyle="bold"
        android:background="@drawable/selector_btn_rectangle"
        android:id="@+id/welcome_close_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="12dp"
        android:layout_marginTop="12dp"
        android:padding="5dip"
        android:textSize="14sp"
        android:textColor="@color/green_common"
        android:text="@string/welcome_paas"
        />

</RelativeLayout>